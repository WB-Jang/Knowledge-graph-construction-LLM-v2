# ==============================================================================
# Minimal dependencies for local CPU testing (OpenRouter single provider)
#
# Install:
#   python -m venv .venv
#   source .venv/bin/activate         # Windows: .venv\Scripts\activate
#   pip install -r requirements.txt
#
# GPU (torch), Korean morph analyzers (konlpy/kiwipiepy), and FastAPI are NOT
# needed by the current pipeline and are excluded. neo4j is only used for the
# optional Memgraph save step. Other providers (Groq/Gemini) live in
# requirements-optional.txt.
# ==============================================================================

# LangChain / LangGraph core
langchain>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0
langgraph>=0.2.0,<0.3.0

# OpenRouter (OpenAI-compatible endpoint)
langchain-openai>=0.2.0,<0.3.0

# Data models / settings
pydantic>=2.5.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0

# Graph database (Memgraph, Bolt protocol) - used only in the save step
neo4j>=5.14.0,<6.0.0

# PDF text extraction
PyMuPDF>=1.23.0,<1.24.0

# HTTP
httpx>=0.26.0,<0.28.0
requests>=2.31.0,<3.0.0

# Console output / progress
rich>=13.7.0,<14.0.0
tqdm>=4.66.1,<5.0.0
