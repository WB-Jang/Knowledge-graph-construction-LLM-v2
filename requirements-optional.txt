# ==============================================================================
# Optional dependencies - install only when needed.
#   pip install -r requirements-optional.txt
# ==============================================================================

# Alternative LLM providers
langchain-groq>=0.2.0,<0.3.0              # Groq (Llama 70B)
langchain-google-genai>=2.0.0,<3.0.0      # Google Gemini
google-generativeai>=0.8.0,<0.9.0

# GPU check (check_gpu). If not installed, the app just prints a CPU-mode notice.
# torch>=2.1.0,<3.0.0

# Graph visualization / analysis
networkx>=3.2.1,<4.0.0
matplotlib>=3.8.2,<4.0.0

# Korean morphological analysis (not used by the current pipeline)
# konlpy>=0.6.0,<0.7.0      # requires Java (JVM)
# kiwipiepy>=0.16.0,<0.17.0
